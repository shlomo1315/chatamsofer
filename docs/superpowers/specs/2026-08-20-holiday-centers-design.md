# חלוקת חגים — מוקדי חלוקה, שוברים וטעינת כרטיסים

**תאריך:** 20.08.2026 · **סטטוס:** אושר לתכנון

---

## 1. מה נבנה

מערכת בחירת מוקד חלוקה לחגי תשרי: המשפחה בוחרת מוקד (בטלפון או בממשק),
מקבלת שובר מעוצב במייל, וכרטיסה נטען ב-500₪ בנדרים.

**זהו ממשק נפרד לחלוטין מיולדות** — מוקדים נפרדים, הרשאות נדרים נפרדות,
שוברים נפרדים. אין שיתוף נתונים בין השניים.

### מה כבר קיים ואין לבנות מחדש

| רכיב | מיקום | מצב |
|---|---|---|
| שלוחה טלפונית לחגים | `app/api/webhooks/yemot-holiday` | עובד — רישום בלבד |
| עריכת הודעות + ElevenLabs | `yemotHolidayMessages` + `yemot-holiday/generate-voice` | עובד |
| הרשאות נדרים לחגים | `getHolidayNedarimCreds`, `getHolidayLimitedId` | קיים ונפרד |
| טעינת כרטיס | `addTlush` | עובד |
| פריקה / ניתוק | `prikatTlush`, `setMagneticCard` | עובד |
| היסטוריית עסקאות | `getClientCardFull` → `History[]` | עובד |
| מנוע שוברים | `maternityVoucher.ts` + `voucherTextsStore` | עובד |
| זיהוי דרכון | `isPassportId` | קיים |
| רשומות חלוקה | `distribution_recipients` (6,046 שורות) | קיים |

---

## 2. מודל הנתונים

### `holiday_centers` — מאגר גלובלי

```sql
id            uuid pk
city          text not null      -- "ירושלים"
name          text not null      -- "אזור נווה צבי"
address       text
phone         text
hours         text               -- שעות הפעילות לשובר
region        text not null      -- 'jerusalem' | 'center' | 'north' | 'south'
capacity      integer            -- null = ללא הגבלה
is_active     boolean default true
sort_order    integer
```

**גלובלי ונשמר מחג לחג.** `city + name` מאפשר כמה מוקדים באותה עיר.
`region` מזין את תפריט האזורים בשלוחה.

### `holiday_center_openings` — מה פתוח בחלוקה הזו

```sql
distribution_id   uuid not null
center_id         uuid not null
capacity_override integer
primary key (distribution_id, center_id)
```

**אילו מוקדים זמינים בחלוקה זו.** הבחירה מתאפסת בכל חלוקה חדשה —
המשפחה בוחרת מחדש בכל חג.

### `holiday_distributions` — עמודה חדשה

```sql
centers_open  boolean not null default false
```

**זה מה שהלשונית "פתח/סגור בחירה" מדליקה** — מתג עצמאי, בלתי תלוי
בשער הרישום. ⚠️ בלעדיו בחירת המוקד הייתה נחסמת ברגע שהרישום נסגר,
כלומר בדיוק כשהיא אמורה להיפתח (ראו §4).

### `holiday_transactions` — מטמון עסקאות

```sql
id            uuid pk
recipient_id  uuid not null
tx_date       timestamptz
store_name    text not null
amount        numeric
raw           jsonb
unique (recipient_id, tx_date, store_name, amount)
```

**🔴 המטמון אינו מותרות.** `getClientCardFull` נקרא לכל משפחה בנפרד.
ביולדות יש עשרות משפחות וזה עובד; כאן יש ~6,000 — כלומר 6,000 קריאות API
בכל פתיחת מסך, דקות ארוכות. תור רקע מסנכרן למסד, והמסך קורא מהמסד.

### `distribution_recipients` — שלוש עמודות חדשות

```sql
center_id         uuid           -- המוקד שנבחר
center_chosen_at  timestamptz
center_source     text           -- 'phone' | 'portal' | 'admin'
load_status       text           -- 'pending' | 'loaded' | 'failed'
load_error        text
loaded_at         timestamptz
```

### אינדקסים

```sql
create index on distribution_recipients (distribution_id, center_id);
create index on distribution_recipients (distribution_id, load_status);
create index on holiday_transactions (recipient_id, tx_date desc);
```

---

## 3. הבחירה — שני ערוצים, מקור אמת אחד

```
טלפון (שלוחה 1) ─┐
                  ├─→ lib/holidayCenterPick.ts ─→ center_id
ממשק דיגיטלי    ─┘      אימות · תקרה · נעילה
```

**שני הערוצים עוברים דרך אותה פונקציה.** זה מה שמונע מצב שבו הטלפון
מאפשר מה שהממשק חוסם.

```ts
pickCenter({ recipientId, centerId, source }): Promise<
  | { ok: true }
  | { ok: false; reason: 'locked' | 'full' | 'closed' | 'not_found' }>
```

**נעילה:** אחרי בחירה — נעול. המשפחה אינה יכולה לשנות בטלפון או בממשק;
רק מנהל, מהטבלה. זה מה שההודעה מבטיחה, וזה מה ששומר על תקרות המוקדים.

⚠️ **הנעילה חלה גם על שלוחה 4** ("שמיעת המוקד שנבחר"). היא קוראת בלבד
ואינה מציעה שינוי — אחרת ההבטחה "לא ניתן יהיה לשנות" נשברת בערוץ צדדי.

### אזהרה לפני האישור — בשני הערוצים

🔴 **לפני** האישור, לא אחריו. אחרי הלחיצה אין מה לעשות עם המידע, ומשפחה
שתגלה את הנעילה רק בדיעבד תתקשר למשרד.

| ערוץ | נוסח |
|---|---|
| שלוחה | "שימו לב, בחירת המוקד היא סופית ואינה ניתנת לשינוי. לאישור הרישום למוקד זה הקישו 1, לחזרה לרשימה הקישו 2" |
| אתר | "הבחירה סופית. לאחר האישור לא ניתן לשנות את המוקד, והשובר יישלח למוקד שנבחר בלבד." |

⚠️ שני נוסחים ולא אחד: הטלפון **נשמע** והאתר **נקרא**. נוסח ארוך בשלוחה
מאבד את המאזין לפני ההוראה עצמה; נוסח טלגרפי במסך נראה מרושל.

### חזרה של מי שכבר בחר

🔴 `locked` אינה הודעת שגיאה אלא **אישור**: מי שמתקשר שוב רוצה לדעת איזה
מוקד בחר, לא לשמוע שנכשל.

> "כבר נרשמתם למוקד החלוקה ב**{מוקד}**. לא ניתן לשנות את הבחירה"

⚠️ נפילה-לאחור כשאין שם מוקד — המשפט נשאר תקין ואינו משמיע `{center}`.

`center_source` מתעד מאיזה ערוץ הגיעה הבחירה (`phone`/`portal`/`admin`).
⚠️ נחוץ לבירורים: "בחרתי בטלפון ורשום אחרת" אינו ניתן לבדיקה בלעדיו.

---

## 4. השלוחה הטלפונית

**✅ שלוחה 6** — אומת בממשק ימות (20.08):

```ini
type=api
api_link=https://chasamsofer.co.il/api/webhooks/yemot-holiday
api_add_0=ApiToken=<YEMOT_WEBHOOK_SECRET>
```

```
שלוחה 6 (נשארת type=api — התפריט בתוך ה-webhook)
  ├─ 1 → רישום           → הזרימה הקיימת, ללא שינוי כלל
  ├─ 2 → חיבור כרטיס      → הזרימה הקיימת
  ├─ 3 → בחירת מוקד       → חדש
  └─ 4 → שמיעת המוקד שנבחר → חדש (מי"ז אלול)
```

**🔴 התפריט ממומש בקוד ולא כ-`type=menu` בימות.** שלוחה 6 נשארת בדיוק
כפי שהיא מוגדרת היום, ואיננו נוגעים בהגדרות ימות כלל.

| | ימות (`type=menu` + 6/1, 6/2) | בקוד ✅ |
|---|---|---|
| שינוי בשלוחה חיה | נדרש | **אין** |
| הקלטות | מנוהלות בימות | במערכת + ElevenLabs |
| סיכון לרישום הקיים | הגדרה מחדש של מסלול פעיל | **אפס** |

⚠️ החוויה למתקשר זהה. ההבדל היחיד הוא מי מנהל את התפריט — ובקוד הוא
נשאר לצד שאר ההודעות שכבר ניתנות לעריכה.

> ⚠️ **המספר אינו קיים בקוד ואי אפשר לגזור אותו ממנו** — הקישור מוגדר
> בצד ימות, והוובהוק אינו יודע מאיזו שלוחה הגיעה הקריאה.
> `scripts/yemot-setup.mjs` משתמש ב-7 כברירת מחדל, וזו אינה ראיה.
> תועד כאן כדי שלא יידרש אימות חוזר.

> 🔴 **ה-ApiToken הוא סוד.** הוא מוגדר גם ב-`YEMOT_WEBHOOK_SECRET`
> ב-Railway, ובלעדיו השלוחה פתוחה לקריאות זרות שיוצרות רישומים
> אמיתיים. אין לצלם או להעתיק את שורת `api_add_0` לשום מקום משותף;
> אם נחשף — להחליף בשני המקומות יחד.

**🔴 התפריט הוא שכבה בלבד.** מסלולי 1 ו-2 ממשיכים לרוץ בדיוק כמו היום.

### 🔴 שער הכניסה — הנקודה שהייתה שוברת את הפיצ'ר

היום `getOpenDistribution()` רץ **בראש הוובהוק**, ואם הרישום סגור השיחה
מתנתקת מיד (`closed` → `hangup`) — לפני שהמשתמש שומע תפריט כלשהו.

⚠️ זהו בדיוק התרחיש המתוכנן: **הרישום נסגר, ורק אז נפתחת בחירת המוקדים.**
אילו השער היה נשאר בראש, מסלול 3 לא היה נגיש כלל — הפיצ'ר היה מת בלי
שום שגיאה גלויה.

**לכן השער יורד לרמת המסלול:**

| מסלול | תנאי הכניסה |
|---|---|
| 1 · רישום | `getOpenDistribution()` — הרישום פתוח (כמו היום) |
| 2 · חיבור כרטיס | כמו היום |
| 3 · בחירת מוקד | **`centers_open`** — מתג עצמאי, לא תלוי ברישום |
| 4 · שמיעת המוקד | `centers_open` או אחרי סגירתה (קריאה בלבד) |

התפריט עצמו מושמע תמיד; כל מסלול בודק את השער שלו ומקריא הודעה ייעודית
כשהוא סגור. כך "הרישום נסגר" ו"בחירת המוקדים נסגרה" הן הודעות נפרדות
ולא מצב אחד מבלבל.

**מתג `centers_open`** יושב על החלוקה (`holiday_distributions`), ומנוהל
מאותה לשונית "פתח/סגור בחירה" במסך החלוקה.

### זרימת בחירת המוקד (3)

```
הקשת ת"ז/דרכון → זיהוי → הודעת הסבר (חסימת דילוג 3 שניות)
  → תפריט אזורים (4 אפשרויות)
    → תפריט ערים באזור
      → ירושלים/ב"ב/בית שמש: תפריט מוקדים · שאר: ישיר
        → "לרישום למוקד זה הקש 1 · לחזרה הקש 2"
          → אישור → נעילה
```

**דרכונים:** ספרות בלבד, בלי אותיות. `isPassportId` כבר מזהה; ההקלטה
תסביר זאת.

**אזורים** (במקום 19 ערים בתפריט אחד — 40 שניות האזנה):
- ירושלים והסביבה · מרכז · צפון · דרום

### 26 המוקדים

| אזור | ערים |
|---|---|
| ירושלים | **ירושלים ×5** (נווה צבי · שמואל הנביא · קרית צאנז-בעלזא · מאה שערים · רמות פולין) · ביתר עילית |
| מרכז | **בני ברק ×4** (סקולוב · חזון איש · ויז'ניץ · רחוב ירושלים) · **בית שמש ×2** (רמה ב' · רמה ד') · מודיעין עילית · אלעד · אשדוד · לוד-אחיסמך · נתניה |
| צפון | חיפה · נוף הגליל · צפת · טבריה · רכסים · עפולה |
| דרום | קרית גת · ערד · אופקים |

### תיקוני נוסח

- "ביתר עלית" ← **ביתר עילית**
- "בו קיימת מוקד חלוקה" ← **שבהן קיים מוקד חלוקה**
- `????` בטקסט הדרכונים ← "הקישו את הספרות בלבד, בלי האותיות"

**כל ההודעות ניתנות לעריכה + ElevenLabs**, כמו ביולדות.

---

## 5. טעינת 500₪

באישור נרשם `load_status='pending'`, ותור רקע מבצע `addTlush`.

- **המסך אינו נתקע** — גם על 500 משפחות
- **כשל באחד אינו מפיל את השאר** — `load_error` לכל שורה
- ניסיונות חוזרים לכשלים זמניים
- עמודת סטטוס: נטען / נכשל / ממתין + מתי

**אישור גמיש:** הכל · קבוצה מסוננת · הוספה ידנית.

---

## 6. השובר

`lib/holidayVoucher.ts` על מנוע היולדות. תוכן וכיתוב נערכים בהגדרות
דרך `voucherTextsStore` הקיים.

מכיל: שם המשפחה · **המוקד שנבחר** · כתובת · ימים ושעות · הדרכת חיבור
הכרטיס. כפתור "שליחת השוברים" שולח לכולם.

---

## 7. איפוס מלא

```
[איפוס מלא] → תצוגה מקדימה:
                • X כרטיסים פעילים
                • ₪Y עדיין טעונים      ← ההתרעה
                • Z ינותקו
              → אישור מפורש → תור רקע
```

`prikatTlush` + ניתוק הכרטיס המגנטי. **הלקוח נשאר בנדרים** עם היסטוריית
העסקאות — בחג הבא מחברים כרטיס חדש בלי להקליד הכול מחדש.

**ידני בלבד. לעולם לא אוטומטי.**

---

## 8. ביצועים

| דרישה | מימוש |
|---|---|
| טבלה מהירה | `useTablePagination` + אינדקסים |
| פילוח מוקדים | **צובר ב-SQL** (`group by center_id`) — לא 6,046 שורות לדפדפן |
| עסקאות | מטמון ב-`holiday_transactions` |
| שליפות כלל-חלוקתיות | `fetchAllRows` — התקרה השקטה תפסה אותנו 5 פעמים |

---

## 9. סדר בנייה

1. טבלאות + אינדקסים + מסך ניהול המוקדים
2. `holidayCenterPick.ts` + טסטים
3. תפריט השלוחה (1/2/3/4) + הקלטות
4. בחירה בממשק הדיגיטלי
5. עמודת מוקד + פילוח + סינון
6. תור טעינה + סטטוס
7. שובר + שליחה מרוכזת
8. מטמון עסקאות + לשונית
9. איפוס מלא

**1–3 הם הליבה** — בלעדיהם אין בחירה.

---

## 10. מה שאינו בהיקף

- שינוי מסלולי הרישום וחיבור הכרטיס הקיימים
- שיתוף נתונים כלשהו עם יולדות
- מחיקת לקוחות מנדרים (ניתוק כרטיס בלבד)
- פריקה אוטומטית בתאריך
