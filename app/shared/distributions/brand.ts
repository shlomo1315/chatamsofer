// ─────────────────────────────────────────────────────────────────────────────
// פלטת המותג של דף השיתוף — מקור אמת יחיד.
//
// הרקע: הצבעים היו כתובים קשיח בעשרות מקומות לאורך המסכים, וכל תיקון
// במקום אחד השאיר את השאר מאחור. התוצאה הייתה דף שנראה כמעט-אחיד — וזה
// בדיוק מה שקורא כזול.
//
// 🔴 הזהב הוא הזהות. הוא נלקח מהלוגו של היכל החתם סופר, ולא מפלטת ברירת
// המחדל של Tailwind — indigo/slate נראים כמו כל מערכת ניהול אחרת, ודף
// השיתוף הוא הפנים שההנהלה והתורמים רואים.
//
// ⚠️ שתי רמות עומק בכוונה: `ink` לטקסט ולמסגרות, `deep` לרקעים מלאים.
// גרסה אחת בלבד הכריחה לבחור בין טקסט קריא לרקע נעים, ובכל מקום נבחר
// אחרת — משם הגיע חוסר האחידות.
// ─────────────────────────────────────────────────────────────────────────────

export const BRAND = {
  /** הזהב הראשי — כותרות, מסגרות פעילות, הדגשות. */
  gold: '#b08d3f',
  /** זהב כהה — טקסט על רקע בהיר, כשצריך ניגודיות אמיתית. */
  goldDeep: '#8a6a24',
  /** זהב בהיר — קווי הפרדה, זוהר, מסגרות עדינות. */
  goldLight: '#d9b95c',
  /** מסגרות ורקעים שקטים. */
  sand: '#e8dfc9',
  /** רקע הדף. */
  cream: '#faf7f0',
  /** טקסט ראשי. */
  ink: '#3a3630',
  /** טקסט משני. */
  inkSoft: '#6b5d3e',
  /** טקסט עמום — תוויות, הערות. */
  inkMuted: '#8a7a56',
} as const

// ─────────────────────────────────────────────────────────────────────────────
// צבעי המחלקות.
//
// ⚠️ כל מחלקה שומרת על הגוון שלה — הוא מה שמאפשר לזהות אותה במבט, וגם
// מתאים לתפקידה (זהב לחלוקות, ירוק ליולדות, כחול להלוואות). אבל הרוויה
// והעומק אוחדו, כך שארבעתן נראות כמשפחה אחת ולא כארבע מערכות שונות.
//
// 🔴 `deep` נוסף כדי שהכרטיס יוכל לשאת רקע מלא: הגרסאות ה-tint הקודמות
// היו בהירות כל כך שהן נעלמו על לבן, והמסך כולו קרא חיוור.
// ─────────────────────────────────────────────────────────────────────────────

export interface DeptTone {
  /** טקסט ואייקונים. */
  ink: string
  /** רקע רך לאזור האייקון. */
  tint: string
  /** מסגרות. */
  ring: string
  /** רקע מלא לכותרת הכרטיס — נושא טקסט לבן. */
  deep: string
}

export const DEPT_TONES: Record<'holidays' | 'maternity' | 'loans' | 'tree', DeptTone> = {
  holidays: { ink: '#9a6b12', tint: '#fdf4e3', ring: '#ecd9ae', deep: '#b08d3f' },
  maternity: { ink: '#0b6a62', tint: '#e8f7f5', ring: '#b5e0da', deep: '#0f766e' },
  loans:     { ink: '#1a45b8', tint: '#eaf1ff', ring: '#c3d4f7', deep: '#1d4ed8' },
  tree:      { ink: '#6d28d9', tint: '#f3ecff', ring: '#d6c4f7', deep: '#7c3aed' },
}
