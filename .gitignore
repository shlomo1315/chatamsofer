# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel (לא בשימוש — הפריסה מתבצעת ב-Railway)
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
.claude/

# ⚠️ קובצי נתונים לייבוא — מכילים ת"ז, טלפונים ומיילים של מוטבים.
# הקובץ נקרא מקומית על ידי scripts/import-legacy-loans.mjs ואינו שייך למאגר.
*.xlsx
~$*.xlsx
# ⚠️ פלט legacy-loans-to-sql.mjs — אותו PII בדיוק, בצורת INSERT.
scripts/legacy-loans-import.sql
