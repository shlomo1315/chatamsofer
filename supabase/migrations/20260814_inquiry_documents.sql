-- ─────────────────────────────────────────────────────────────────────────────
-- מסמכים שנוספו במהלך הבירור.
--
-- הרקע: בבירור מבקשים מהמשפחה מסמך שחסר (חוזה, אישור, ספח). המסמך הגיע
-- כתשובה בשרשור והיה נשאר שם בלבד — המזכיר שפתח את "מסמכים מצורפים"
-- לא ראה אותו, ולא היה סימן לכך שהמסמך החסר הושלם.
--
-- ⚠️ המסמך נוסף ל-document_urls של ההלוואה (אותו מקום כמו שאר המסמכים),
-- ומסומן ב-added_in_inquiry כדי שיוצג עם התווית "הושלם בתהליך הבירור".
-- הסימון יושב בתוך ה-JSON של כל מסמך ולא כעמודה נפרדת, כי הוא מאפיין
-- של המסמך הבודד ולא של הבקשה.
-- ─────────────────────────────────────────────────────────────────────────────

-- קישור המסמך שצורף להודעת בירור — לתצוגה בתוך השרשור עצמו.
alter table public.loan_messages
  add column if not exists attachment_url text;

alter table public.loan_messages
  add column if not exists attachment_name text;

comment on column public.loan_messages.attachment_url is
  'מסמך שצורף להודעה בשרשור הבירור. נוסף גם ל-document_urls של ההלוואה.';
