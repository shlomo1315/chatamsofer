-- ─────────────────────────────────────────────────────────────────────────────
-- 🔴 מוקד שסיים לחלק — בשונה ממוקד שטרם החל.
--
-- ⚠️ pickup_open_at מתאפס ל-NULL בסגירה, ולכן "טרם החל" ו"כבר סיים" נראים
-- זהים במסד. בטלפון שניהם השמיעו "המוקד טרם החל לחלק" — כלומר משפחה
-- שאיחרה נשלחה להמתין להודעה שלא תגיע, במקום לדעת שהמועד עבר.
--
-- ⚠️ חותמת ולא דגל בוליאני: השעה המדויקת נדרשת לבירור מול משפחה שטוענת
-- שהגיעה בזמן.
-- ─────────────────────────────────────────────────────────────────────────────
ALTER TABLE public.holiday_center_openings
  ADD COLUMN IF NOT EXISTS pickup_ended_at timestamptz;

COMMENT ON COLUMN public.holiday_center_openings.pickup_ended_at IS
  'מתי המוקד סיים לחלק כרטיסים. NULL = טרם סיים. נקבע בסגירת מוקד שכבר החל.';
