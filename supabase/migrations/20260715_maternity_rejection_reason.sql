-- ─────────────────────────────────────────────────────────────────────────────
-- סיבת דחייה לתיק יולדת.
--
-- כשדוחים בקשת לידה (סטטוס 'cancelled') — בין אם מבקשה ממתינה ובין אם מבטלים
-- לידה שכבר אושרה — המזכיר מזין סיבה. הסיבה נשמרת כאן ונשלחת ליולדת במייל מעוצב.
-- ─────────────────────────────────────────────────────────────────────────────

alter table public.maternity_aids
  add column if not exists rejection_reason text;
