-- ============================================================================
-- Migration: שדה הסבר לבדיקה מעמיקה (deep_review_reason)
-- ============================================================================
--
-- כשמזכיר מעביר מוטב ל"בדיקה מעמיקה" (העבר לאישור מנהל לאחר בדיקת מסמכים),
-- הוא כותב שורת הסבר למה היחוס דורש בדיקה. ההסבר נשמר כאן ומוצג למנהל
-- האחראי על הבדיקה המעמיקה.
--
-- 🔒 אידמפוטנטי. ▶ הרצה: Supabase → SQL Editor → Run.
-- ============================================================================

alter table public.beneficiaries
  add column if not exists deep_review_reason text;
