-- ============================================================================
-- Migration: סימוני צבע ידניים לדורות (lineage_manual_marks)
-- ============================================================================
--
-- בעץ הדורות, המנהל יכול לסמן ידנית כל דור באדום (חורג) או ירוק (מאושר),
-- מעבר לצביעה האוטומטית (ירוק=תואם עד דור 5, אדום=שונה עד דור 5, כתום=השאר).
-- הסימונים נשמרים כאן כ-JSON: { "3": "red", "6": "green" } (דור → צבע).
--
-- 🔒 אידמפוטנטי. ▶ הרצה: Supabase → SQL Editor → Run.
-- ============================================================================

alter table public.beneficiaries
  add column if not exists lineage_manual_marks jsonb;
